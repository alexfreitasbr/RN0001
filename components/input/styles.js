import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({

  containerInput: {
    flexDirection: "row",
    gap: 10,
    justifyContent: "center",
  },
  textInput: {
    flex: 1,
    borderRadius: 8,
    borderColor: "gray",
    borderWidth: 1,
    paddingHorizontal: 8,
  },
  button: {
    borderRadius: 20,
    backgroundColor: "red",
  },

});